<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdvopsv1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layoutIngresarTratamiento" />
        <activity android:name=".declarar_toma_medicamentos" />
        <activity android:name=".doctoresMiperfil" />
        <activity android:name=".medicamentosMiperfil" />
        <activity android:name=".tratamientoMiperfil" />
        <activity android:name=".usuarioEsposa" />
        <activity android:name=".usuarioMama" />
        <activity android:name=".usuarioHijo" />
        <activity android:name=".usuarioMiPerfil" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Usuarios" />
    </application>

</manifest>